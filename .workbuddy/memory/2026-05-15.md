# 2026-05-15

## 图片引用本地化（已回滚）

将 newYearFlag.wxss 和 newYearFlag.wxml 中所有服务器图片 URL 批量替换为本地路径：
- `https://6f6e-online-e879a3-1258444488.tcb.qcloud.la/newYearFlag/` → `/pages/newYearFlag/images/`
- `https://www.sunniejs.cn/static/flag/` → `/pages/newYearFlag/images/settings/`
- 注意：`snow/`（1~20帧）和 `fireworks/`（1~38帧）序列帧图片本地没有，需要用户手动补充到对应目录。

### ⚠️ 本地路径回滚

微信小程序 WXSS 不支持本地路径的 `background-image`，只能用网络图片或 base64。
已将 WXSS 和 WXML 中所有本地路径恢复为远程 CDN 地址（194处 WXSS + 1处 WXML）。

**重要教训**：小程序中 `background-image` 只能用远程 URL 或 base64，本地图片只能通过 `<image>` 标签引用。

---

## flag 小程序修复

修复了 pages/newYearFlag/newYearFlag.js 中的多个 bug：

1. **`wx.createCanvasContext` 模块级调用问题**：将其移到 `onReady` 生命周期中初始化，避免页面未渲染就调用。
2. **`handlePainter` 逻辑错误**：原代码中 `if(this.data.readCheck)` 判断反了（勾选才执行vs未勾选才提示），已修正为先校验、未勾选则显示动画提示，已勾选再执行倒计时+画图。
3. **`wx.playBackgroundAudio` 废弃 API**：替换为 `wx.getBackgroundAudioManager()`，用 try/catch 包裹防止崩溃。
4. **WXML intro 按钮绑定错误**：intro 页面"开始"按钮原来绑定了 `handlePainter`，改回正确的 `introGoTap`，使流程能正常跳转到 select 步骤。
5. **`tempFlagLists` 初始值**：将 `data.tempFlagLists` 初始值由带测试数据改为空数组 `[]`，避免初始就显示假数据。
